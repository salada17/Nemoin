# nemoin

1. cmd : 해당 파일경로에서 npm install.
2. 혹시나, styled components file이 오류라고 뜨면 @types/styled-components로 수정했다가 다시 styled-components로 저장.
3. visual studio에서 해당 솔루션을 build합니다. localhost:5000이 정상적으로 떠야 합니다. 
4. cmd : npm run build.
5. cmd : npm start.
5. 브라우저에서 http://localhost:8000/ 에 접속.